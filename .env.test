# Commité, comme `.env`. Next ne lit pas `.env.local` en mode test : les tests
# ne peuvent donc pas hériter d'une base tirée de Vercel, et prennent la leur ici.
DATABASE_URL="postgresql://postgres:postgres@localhost:5432/nutrition-calculator-test"
